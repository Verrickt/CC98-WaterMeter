<ui:FluentWindow  x:Class="WaterMeter.StatResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:WaterMeter"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        FontFamily="Microsoft YaHei"
        Icon="pack://application:,,,/Assets/98icon.png"
        FontSize="8"
        Title="统计结果" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ui:TitleBar Title="统计结果"
                     >
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/98icon.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto">
            <TextBox IsReadOnly="True" Text="{Binding UBBCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"></TextBox>
        </ScrollViewer>
        <StackPanel  Grid.Row="2" >
            <Button Padding="5"
                    Margin="10"  HorizontalAlignment="Center" Command="{Binding CopyToClipboardCommand}">复制到剪切板</Button>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
